# Supabase project (Settings -> API). Leave both empty to run in local-only mode:
# data stays on this device (IndexedDB) and nothing syncs.
# The anon key is a public client key protected by row-level security; never put the service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
